"""web/ui -- MarketScans designsystem (enda källan för utseende).

Moduler:
    tokens     -- designtokens (färg, spacing, typografi, radius)
    css        -- global CSS byggd från tokens
    icons      -- koncept -> Material Symbols-ikon
    glossary   -- central ordlista + info-bubblor ("bra värde"-vägledning)
    components -- återanvändbara byggblock (page_header, metric_card, kpi_grid, …)
    ai_action  -- AI-djupväljare vid användningsstället (ersätter global väljare)
"""
