# === Runtime dependencies ===
# finvizfinance: gratis Finviz-screener (universe_discovery.py).
# Mjuk dep — systemet fallback-ar graciöst om paketet saknas.
finvizfinance>=1.3.0,<2.0.0
# reticker: robust ticker-extrahering ur fritext (news_sentiment.py).
# Mjuk dep — faller tillbaka till regex om ej installerat.
reticker>=0.2.0
# vaderSentiment: fallback-sentimentanalys när FinBERT ej är tillgängligt.
# Lättviktig (inga ML-beroenden).
vaderSentiment>=3.3.2
# Pinnar med övre gränser för att skydda mot breaking changes i minor releases.
# Pillow + cryptography listas INTE explicit — de är transitive deps från
# streamlit/PyNaCl och kan trigga pip-resolver-backtracking om de fästs här.
# curl_cffi listas INTE explicit — yfinance hanterar sin egen curl_cffi-dep;
# en explicit pinn konfliktar med nyare yfinance-versioner och hänger Streamlit Cloud.
mistune>=3.0.0,<4.0.0
yfinance>=0.2.40,<0.3.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.2.0
requests>=2.32.0,<3.0.0
ddgs>=9.0.0,<10.0.0
tabulate>=0.9.0,<1.0.0
python-dateutil>=2.8.2,<3.0.0
flask>=3.0.0,<4.0.0
openpyxl>=3.1.0,<4.0.0
python-dotenv>=1.0.0,<2.0.0
PyNaCl>=1.5.0,<2.0.0
streamlit>=1.40.0,<2.0.0
bcrypt>=4.0.0,<5.0.0
# E7: Pydantic Settings för centraliserad konfiguration (mjuk dependency — faller tillbaka om saknas)
pydantic-settings>=2.0.0,<3.0.0
plotly>=5.18.0,<6.0.0
scipy>=1.11.0,<2.0.0

# === ML-stack (runtime: bara loading + inference) ===
# xgboost+sklearn behövs för att unpickla modellen och köra .predict().
# scipy/pyarrow behövs ENDAST för träning → installeras i train_ml.yml.
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0
joblib>=1.3.0,<2.0.0

# === Dev/test dependencies ===
# Installera med: pip install -r requirements.txt[dev]
# eller: pip install -e ".[dev]"
pytest>=8.0
pytest-cov>=5.0
ruff>=0.11.0
mypy>=1.15.0
pre-commit>=4.0
types-requests
types-python-dateutil
